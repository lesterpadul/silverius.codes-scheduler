<%= form_with(model: scheduled_email, local: true) do |form| %>
  <% if scheduled_email.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(scheduled_email.errors.count, "error") %> prohibited this scheduled_email from being saved:</h2>

      <ul>
      <% scheduled_email.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :status %>
    <%= form.number_field :status %>
  </div>

  <div class="field">
    <%= form.label :subject %>
    <%= form.text_field :subject %>
  </div>

  <div class="field">
    <%= form.label :content_text %>
    <%= form.text_field :content_text %>
  </div>

  <div class="field">
    <%= form.label :content_html %>
    <%= form.text_field :content_html %>
  </div>

  <div class="field">
    <%= form.label :repeating_event %>
    <%= form.number_field :repeating_event %>
  </div>

  <div class="field">
    <%= form.label :scheduled_date %>
    <%= form.date_select :scheduled_date %>
  </div>

  <div class="field">
    <%= form.label :scheduled_time %>
    <%= form.time_select :scheduled_time %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
